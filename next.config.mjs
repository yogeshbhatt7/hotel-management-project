/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['avatars.githubusercontent.com','plus.unsplash.com', 'images.unsplash.com'],
    }
};

export default nextConfig;
